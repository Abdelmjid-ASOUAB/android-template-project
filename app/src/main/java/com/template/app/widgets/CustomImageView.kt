package com.template.app.widgets

import android.util.AttributeSet
import androidx.appcompat.widget.AppCompatImageView

class CustomImageView(context: android.content.Context, attrs: AttributeSet) :
    AppCompatImageView(
        context,
        attrs
    ) {
    init {
    }
}